

def normalize(text: str):
    return

def add_comment(para, comments, comment_id):
    return
    
def find_comment_in_doc(doc, citations):
    
    # extract paragraph content and normalize
    
    # normalize citations
    
    # all_text = para + citations
    
    # embedding all_text with transformer
    
    # retrieve vector for para
    # retrieve vector for citations
    
    # for loop with cosine similarity
    # take best score, para content, index, citation
    
    return


def annotate_doc(doc, citations):
    
    # citation_found = find_comment_in_doc(doc, citations)
    # for cit in citation_found:
    # get index
    # get citation
    # select correct paragraph to put index
    # add_comment(doc.paragraphs[index], citation)
    #
    
    return
    
def annotate_docx(file):
    
    # doc = Document(file)
    
    # analysis = graph() (import graph from supervisor architecture)
    
    # citations = analysis[citations]
    
    # annotate_doc(doc, citation)
    
    return 